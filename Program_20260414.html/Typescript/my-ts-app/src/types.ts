// types.ts
// export the different types needed for the application

export type User = {
    id: number;
    name: string;
    role: string;
}

export type Address = {
    street: string;
    city: string;
    country: string;
}